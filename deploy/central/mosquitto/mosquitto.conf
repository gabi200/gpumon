listener 1883
persistence true
persistence_location /mosquitto/data/

# Demo config: anonymous access. For production, disable this and add a
# password file:
#   allow_anonymous false
#   password_file /mosquitto/config/passwd
# then create users with:  mosquitto_passwd -c /mosquitto/config/passwd gpumon
allow_anonymous true
